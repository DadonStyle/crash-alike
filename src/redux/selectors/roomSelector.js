const isRoundOver = (state) => state.room.isRoundOver;
const isWaitingRoom = (state) => state.room.isWaitingRoom;
const isGameStarted = (state) => state.room.isGameStarted;

export default { isRoundOver, isWaitingRoom, isGameStarted };
