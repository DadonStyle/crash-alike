const clientObj = (state) => state.connection.clientObj;
const socket = (state) => state.connection.socket;
const msgArr = (state) => state.connection.msgArr;

export default { clientObj, socket, msgArr };
